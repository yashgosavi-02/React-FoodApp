const heading = React.createElement('h1', {id:'heading'}, 'Namaste React 🚀');
const root = ReactDOM.createRoot(document.getElementById('root'));

// heading and root are React elements which are objects
root.render(heading);
